apiVersion: v1
kind: Pod
metadata:
  name: known-vulnerable
  namespace: platform
spec:
  containers:
    - name: app
      image: alpine:3.18
      command: ["/usr/bin/openssl"]
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
